notifications:
  email:
    recipients:
      - dev@rocketmq.apache.org
  on_success: change
  on_failure: always

language: java

matrix:
  include:
  # On OSX, run with default JDK only.
  # - os: osx
  # On Linux, run with specific JDKs only.
  - os: linux
    env: CUSTOM_JDK="oraclejdk8"

before_install:
  - wget https://github.com/apache/rocketmq/archive/a6fb3f8ced94b0b8b3300f3bbb9d593546e1e767.zip
  - unzip a6fb3f8ced94b0b8b3300f3bbb9d593546e1e767.zip
  - cp -rT rocketmq-a6fb3f8ced94b0b8b3300f3bbb9d593546e1e767 ./
  - rm -r rocketmq-a6fb3f8ced94b0b8b3300f3bbb9d593546e1e767
  - rm a6fb3f8ced94b0b8b3300f3bbb9d593546e1e767.zip
  - echo 'MAVEN_OPTS="$MAVEN_OPTS -Xmx1024m -XX:MaxPermSize=512m -XX:+BytecodeVerificationLocal"' >> ~/.mavenrc
  - cat ~/.mavenrc
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then export JAVA_HOME=$(/usr/libexec/java_home); fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then jdk_switcher use "$CUSTOM_JDK"; fi

script:
  - travis_retry mvn -B clean apache-rat:check
  - travis_retry mvn -B package jacoco:report coveralls:report

after_success:
  - mvn clean install -Pit-test
  - mvn sonar:sonar -Psonar-apache
